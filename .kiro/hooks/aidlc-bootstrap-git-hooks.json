{
  "version": "v1",
  "hooks": [
    {
      "name": "aidlc-bootstrap-git-hooks (command, no agent) — checks .githooks/pre-commit exists + core.hooksPath is set; silent no-op if so (the common case, every save), restores from HEAD if not",
      "trigger": "PostFileSave",
      "action": {
        "type": "command",
        "command": "bash scripts/bootstrap-git-hooks-check.sh"
      },
      "timeout": 10,
      "enabled": true
    }
  ]
}
