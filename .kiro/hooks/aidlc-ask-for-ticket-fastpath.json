{
  "version": "v1",
  "hooks": [
    {
      "name": "Empty-ticket fast path (command, no agent) — blocks cheaply before aidlc-ask-for-ticket-if-missing.json's agent hook has to",
      "trigger": "UserPromptSubmit",
      "action": {
        "type": "command",
        "command": "bash scripts/ticket-gate-fastpath.sh"
      },
      "timeout": 10,
      "enabled": true
    }
  ]
}
